apiVersion: v1
kind: Service
metadata:
  name: {{ template "managerworkercreator.fullname" . }}
  labels:
    app: {{ template "managerworkercreator.name" . }}
    chart: {{ template "managerworkercreator.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "managerworkercreator.name" . }}
    release: {{ .Release.Name }}
